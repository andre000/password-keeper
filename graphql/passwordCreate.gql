mutation PasswordCreate($title: String!, $username: String!, $icon: String!, $fields: [FieldInput]!) {
  addPassword(title: $title, username: $username, icon: $icon, fields: $fields) {
    _id
    title
    username
    icon
    fields {
      title
      value
    }
  }
}
